{
  "$id": "session-task",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Session Task",
  "description": "A session-scoped task card with status transitions.",
  "type": "object",
  "required": ["task_id", "title", "status"],
  "properties": {
    "task_id": {
      "type": "string",
      "description": "Unique identifier for the task."
    },
    "title": {
      "type": "string",
      "description": "Display title of the task."
    },
    "status": {
      "type": "string",
      "enum": ["pending", "in_progress", "completed", "failed", "canceled", "cancelled"],
      "description": "Current task status. Use \"canceled\" for new payloads. The legacy \"cancelled\" spelling remains readable through go-envelopes v0.4.x and is scheduled for removal in v0.5.0."
    },
    "description": {
      "type": "string",
      "description": "Optional description shown below the task title."
    }
  },
  "additionalProperties": false
}
